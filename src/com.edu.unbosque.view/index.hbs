<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cuevanita1.0</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
     <h2>Lista de Películas</h2>

<!-- Mostrar la lista de películas -->
  <ul>
      {{#each peliculas}}
        <div>
            <h3>{{titulo_p}} ({{ano_produccion}})</h3>
            <p>Título Secundario: {{titulo_s}}</p>
            <p>Nacionalidad: {{nacionalidad}}</p>
            <p>Presupuesto: ${{presupuesto}}</p>
            <p>Duración: {{duracion}} minutos</p>
            <a href="/peliculas/editar/{{_id}}">Editar</a> | 
            <a href="/peliculas/eliminar/{{_id}}" onclick="return confirm('¿Estás seguro de que quieres eliminar esta película?');">Eliminar</a>
        </div>
      {{/each}}
  </ul>


  <!-- Formulario para agregar nueva película -->
  <h3>Agregar Nueva Película</h3>
<form action="/peliculas" method="POST"> <!-- Corregir la sintaxis del formulario -->
    <label for="cip">CIP (Código de película):</label>
    <input type="text" name="cip" id="cip" required maxlength="10" />

    <label for="titulo_p">Título de la película:</label>
    <input type="text" name="titulo_p" id="titulo_p" required maxlength="45" />

    <label for="ano_produccion">Año de producción:</label>
    <input type="number" name="ano_produccion" id="ano_produccion" required min="1900" />

    <label for="titulo_s">Título secundario:</label>
    <input type="text" name="titulo_s" id="titulo_s" />

    <label for="nacionalidad">Nacionalidad:</label>
    <input type="text" name="nacionalidad" id="nacionalidad" />

    <label for="presupuesto">Presupuesto:</label>
    <input type="number" name="presupuesto" id="presupuesto" required min="1" />

    <label for="duracion">Duración (minutos):</label>
    <input type="number" name="duracion" id="duracion" required min="1" />

    <button type="submit">Agregar Película</button>
</form>
</body>
</html>